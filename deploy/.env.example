# Copie en `deploy/.env`. Lu par docker compose lui-meme (domaine, base).
# Les reglages applicatifs vivent dans .env.prod et .env.staging.

# Sans sous-domaine ni protocole. Pour tester avant propagation du DNS :
# DOMAINE=178.32.16.63.nip.io
DOMAINE=faridb.site

# Adresse utilisee par Let's Encrypt pour prevenir d'une expiration.
EMAIL_ACME=f_benamara@hetic.eu

POSTGRES_USER=meglabs
POSTGRES_PASSWORD=change-moi-par-une-chaine-longue-et-aleatoire
